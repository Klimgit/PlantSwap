package com.plantswap.listings.domain.model;

public enum ListingType {
    /** Отдам бесплатно. */
    GIVE_AWAY,
    /** Обменяю на другое растение. */
    SWAP,
    /** Продам за деньги — требует заполнения цены. */
    SELL
}
